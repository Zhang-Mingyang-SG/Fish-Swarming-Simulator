# Fish-Swarming-Simulator
Unity 6 boids simulator scaling to 1,000,000 fish — swap between single-threaded, Burst-parallel, and GPU-compute backends at runtime and watch the cost live in a built-in profiler.
